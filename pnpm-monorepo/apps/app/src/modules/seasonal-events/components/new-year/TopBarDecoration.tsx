/**
 * New Year has no decoration of the top bar, thus the slot stays empty. The
 * entry keeps the shape of the other themes, so that the top bar needs no
 * case for an event without it.
 */
export const NewYearTopBarDecoration = () => null;
