/**
 * New Year has no decoration of the SpyNet search tile, thus the slot stays
 * empty. The entry keeps the shape of the other themes, so that the
 * dashboard needs no case for an event without it.
 */
export const NewYearSpynetSearchTileDecoration = () => null;
