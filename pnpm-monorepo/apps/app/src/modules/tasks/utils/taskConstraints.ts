/**
 * Arbitrary (untested) cap so a hostile client cannot store an unbounded
 * description. Shared by the task actions and the description fields of the
 * task forms.
 */
export const TASK_DESCRIPTION_MAX_LENGTH = 2048;
